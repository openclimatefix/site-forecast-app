We have three different test folders:

- Unit: These tests focus on individual functions in isolation and verify that each function behaves as expected.
- Integration: These tests cover interactions between functions and external dependencies. Mocking is used where possible to control external factors.
- End-to-End: These tests validate the entire application flow to ensure everything works together correctly. They do not connect to any live environments.